// API used for getting recipe data for our app
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';

// timeout value for calling the timeout function if data is not fetched after a certain time
export const TIMEOUT_SECONDS = 10;

// number of data to display per page when the search data has been returned
export const RESULT_PER_PAGE = 10;
